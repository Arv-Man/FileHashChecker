# FileHashChecker
A simple Windows PowerShell Drag-and-drop file hash calculator with optional checksum verification

Change the Desktop Shortcut Target to= powershell.exe -NoProfile -ExecutionPolicy Bypass -Command "& '{filelocation}.ps1' @args"

For even better integration, add to your sendto menu!  open run and type shell:sendto
